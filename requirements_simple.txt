# Object detection and transformer libraries
transformers>=4.0.0
datasets>=1.0.0
torch>=1.9.0
torchvision>=0.10.0
timm>=0.4.5
torchmetrics

# Optional: Specify versions if needed or remove if using latest versions automatically
# If there's any specific version of a library you need, you can specify it like this:
# transformers==4.25.1
# datasets==2.6.1
# torch==1.12.0
# torchvision==0.13.0
# timm==0.6.12
